# a python code that can be used to replace errors in sentences 

def replace_word():

    str = "In Kenya, you will never lack problems to solve"
    word_to_replace = input("Enter the word to replace:")
    word_replacemet = input("Enter the word replacement:")
    
    print(str.replace(word_to_replace,word_replacemet))

replace_word()